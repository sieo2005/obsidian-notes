# 强制规则：数据治理

## 基本原则
**数据不删除，只转移**

## 可删除条件
1. 用户明确说“删除/不要了/没用了”
2. 数据完全重复（去重）
3. 用户确认垃圾数据

其他情况：只能转移，不能删除。

## 任务归档机制
目录：`任务归档/YYYY/`

建议执行：
- 会话结束时检查任务清单中 [x] 任务
- 转移到当月归档文件
- 保留元数据：类型、来源、创建日期、完成日期、成果

## 对话存档
- 使用 `对话历史.md` 进行会话级追加记录
- 禁止删除和覆盖历史
